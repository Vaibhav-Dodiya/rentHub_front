{
  "buildCommand": "flutter/bin/flutter build web --release --dart-define=API_BASE_URL=https://renthub-4.onrender.com",
  "outputDirectory": "build/web",
  "installCommand": "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable; fi && flutter/bin/flutter config --no-analytics && flutter/bin/flutter precache --web && flutter/bin/flutter pub get",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
