{
  "buildCommand": "ls -la build/web",
  "outputDirectory": "build/web",
  "installCommand": "echo 'No install needed - using pre-built files'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}